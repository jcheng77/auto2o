<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @deal.user_id %>
</p>

<p>
  <strong>Tender:</strong>
  <%= @deal.tender_id %>
</p>

<p>
  <strong>Bid:</strong>
  <%= @deal.bid_id %>
</p>

<p>
  <strong>Bargain:</strong>
  <%= @deal.bargain_id %>
</p>

<p>
  <strong>Dealer:</strong>
  <%= @deal.dealer_id %>
</p>

<p>
  <strong>Final price:</strong>
  <%= @deal.final_price %>
</p>

<p>
  <strong>Postscript:</strong>
  <%= @deal.postscript %>
</p>

<!-- <%= link_to 'Edit', edit_deal_path(@deal) %> |
<%= link_to 'Back', deals_path %> -->

<%= link_to '取消购买意向', cancel_1_round_tender_path(@tender), method: :delete, data: { confirm: 'Are you sure?' }  if ['intention', 'determined','qualified', 'invite', 'bid_open', 'bid_closed', 'deal_closed'].include?(@tender.state) %>

<%= link_to '取消购买意向', cancel_2_round_tender_path(@tender), method: :delete, data: { confirm: 'Are you sure?' }  if ['bargain_started', 'final_bid_open', 'final_bid_closed', 'final_deal_closed'].include?(@tender.state) %>