<%= form_for(@bargain, url: submit_bargain_tender_path(@tender)) do |f| %>
  <% if @bargain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bargain.errors.count, "error") %> prohibited this tender from being saved:</h2>

      <ul>
      <% @bargain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :postscript %><br>
    <%= f.text_field :postscript %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
